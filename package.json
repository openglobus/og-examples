{
  "name": "og-examples",
  "version": "1.0.0",
  "description": "Openglobus Examples",
  "main": "index.js",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "serve" : "ws",
    "sub": "git submodule update --progress --init --recursive --force --remote",
    "build" : "cd ./external/og && npm install && npm run build",
    "dev" : "cd ./external/og && npm install && npm run dev"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "local-web-server": "^5.3.0"
  }
}
